export type ShrinkImageOptions = {
  quality: number,
}

export function shrinkImage(_image: HTMLImageElement, _options: ShrinkImageOptions): HTMLImageElement {
  const result = new Image();

  return result;
}
