import { defineModule } from "../.vitepress";

export const moduleVariables = defineModule({
  title: "Constants",
  description: "Constants for working with objects.",
  symbols: [
    { name: "DEFAULT_INTERACTIVE_SELECTORS", kind: "variable" },
  ],
});
