import type { Meta } from "../../_internal/meta";

export const meta = {
  id: "getRandomNumber",
  name: "getRandomNumber",
  description: "Returns a random integer within the given inclusive range.",
  category: "number",
  tags: ["random", "integer", "range", "generation", "math"],
  demo: false,
  snippets: true,
  since: "1.0.0",
} satisfies Meta;
