import type { Meta } from "../../_internal/meta";

export const meta: Meta = {
  id: "useProxyValue",
  name: "useProxyValue",
  kind: "composable",
  description:
    "Creates a buffered proxy ref that stages changes before committing back to a source ref, with optional auto-sync and debounce.",
  tags: ["model", "v-model", "proxy", "buffer", "form", "controlled", "debounce"],
  snippets: true,
  demo: false,
  since: "1.0.0",
};
