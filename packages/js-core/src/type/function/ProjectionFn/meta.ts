import type { Meta } from "../../_internal/meta";

export const meta = {
  id: "ProjectionFn",
  name: "ProjectionFn",
  description: "Represents a projection function.",
  category: "type",
  tags: ["type", "function", "projection", "mapping", "transformation"],
  demo: false,
  snippets: false,
  since: "1.0.0",
} satisfies Meta;
