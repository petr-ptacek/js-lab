# Function: add()

> **add**(`a`, `b`): `number`

Adds two numbers together.

## Parameters

### a

`number`

The first number.

### b

`number`

The second number.

## Returns

`number`

The sum of `a` and `b`.

## Example

```ts
add(1, 2); // 3
```
