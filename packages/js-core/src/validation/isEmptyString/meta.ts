import type { Meta } from "../../_internal/meta";

export const meta = {
  id: "isEmptyString",
  name: "isEmptyString",
  description: "Checks whether the provided value is the empty string literal.",
  category: "validation",
  tags: ["validation", "type-guard", "string", "literal"],
  demo: false,
  snippets: true,
  since: "1.0.0",
} satisfies Meta;
