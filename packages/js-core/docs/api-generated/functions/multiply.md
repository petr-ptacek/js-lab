# Function: multiply()

> **multiply**(`a`, `b`): `number`

Multiplies two numbers.

## Parameters

### a

`number`

The first number.

### b

`number`

The second number.

## Returns

`number`

The product of `a` and `b`.

## Example

```ts
multiply(2, 3); // 6
```
